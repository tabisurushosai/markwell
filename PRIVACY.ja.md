# Markwell プライバシーポリシー

**施行日:** 2026-05-18

Markwell は、Web 上のテキストをハイライトし、任意で AI 機能を使える Chrome 拡張機能です。本ポリシーは、現行実装に基づき、どのデータがどこに扱われるかを説明します。

## Markwell が収集する情報

**Markwell は自社サーバー上で個人情報を収集しません。**

ハイライト、タグ、プロジェクト、合成結果、設定、ライセンス状態、および拡張機能が利用する端末識別子は、すべてお使いの端末の **`chrome.storage.local`** にのみ保存されます。Markwell はユーザーアカウント用のデータベースを運用せず、ハイライトをクラウドに同期しません。

## 外部へのデータ送信

データが端末の外に出るのは、次の場合に限られます。

### Google Gemini API（任意）

Options で Gemini API キーを設定し、AI 機能を利用する場合:

- **送信内容:** ハイライト本文（`selected_text`）およびハイライトに付けた注釈（メモ、`note`）。合成・翻訳・言い換え・Q&A・引用抽出・ページ要約・ファクトチェック・関連ハイライトなど、各 AI 機能用にこれらから組み立てたプロンプトを含みます。
- **送信タイミング:** Gemini を呼び出す AI 機能を実行したときのみ。
- **送信先:** リクエストは **お使いのブラウザから Google へ直接**（`generativelanguage.googleapis.com`）送信されます。API キーはそのリクエストに含まれます。**Markwell のサーバーを経由せず**、Markwell は API キーを受け取りません。

API キーを設定しない場合、拡張機能は Gemini API を呼び出しません。

### markwell-api.vercel.app（Premium ライセンスのみ）

Premium ライセンスキーを検証するとき（有効な Premium 向けの定期再検証を含み、おおよそ 7 日ごと）:

- **送信内容:** ライセンスキーと、`chrome.storage.local` に生成・保存されたデバイス ID。
- **送信タイミング:** Options でライセンスキーを適用したとき、および Premium が有効な間のスケジュールされた再検証時。
- **エンドポイント:** `https://markwell-api.vercel.app/api/verify-license`

このサービスへは、ハイライト本文・注釈・Gemini API キーは送信されません。

Premium の購入は Stripe のサイトをブラウザで開く流れであり、その処理は Stripe のプライバシーポリシーの対象です。

## ブロック対象ドメイン・URL パターン

Options で **ブロックするドメイン** および **ブロックする URL パターン** を指定できます。一致するページでは Markwell の content script は動作しません。選択ツールバーの表示、ページテキストとの相互作用、当該ページでのハイライト記録は行われず、**それらのサイト上の内容は Markwell によってローカルに記録されません**。

## クッキー

Markwell はクッキーを使用しません。

## 第三者への提供

Markwell はデータを販売しません。拡張機能が利用する外部送信先は、**Google**（ご自身の API キーで Gemini を利用した場合）、**markwell-api.vercel.app**（上記のライセンス検証）、および **Stripe**（Premium 決済ページを開くことを選択した場合）で、いずれも該当機能を利用した場合に限られます。

## データの削除

- **拡張機能内:** Options → データ から、ローカルデータのエクスポートまたは削除ができます。全データ削除は `chrome.storage.local` からハイライト・タグ・プロジェクト・合成・設定などを削除します（ライセンス関連フィールドの扱いは画面の説明に従います）。
- **アンインストール:** Chrome から拡張機能を削除すると、Markwell の `chrome.storage.local` データは自動的に削除されます。

## お問い合わせ

本ポリシーに関するお問い合わせ: **tabisurushosai+markwell@gmail.com**
